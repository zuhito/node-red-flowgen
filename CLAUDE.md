# CLAUDE.md

Node-RED プラグイン。API 定義（OpenAPI 3 / Swagger 2 / Bruno）を読み、function ノードの**コード文字列**とフローを生成する。

**作業前に `CODING-STANDARDS.md` を読むこと。** 規約と、このリポジトリで繰り返している失敗が書いてある。

## ファイル

| ファイル | 役割 |
| --- | --- |
| `flowgen.js` | 生成器。Node とブラウザ両方で動く UMD |
| `flowgen-plugin.js` | バックエンド。**Node-RED と同一プロセス**で動く |
| `flowgen-plugin.html` | エディタ側 UI。**ES5 で書く** |

## 最優先事項

**エディタが壊れることが最悪の事故。** フローを開発する手段そのものが失われ、稼働中のシステムを直せなくなる。生成コードの誤りは1フローに閉じるが、エディタの障害はすべてを止める。

エディタ側を変更するときの判断順序：

1. その変更が失敗したとき、エディタは生き残るか
2. 全対応ブラウザで構文エラーにならないか
3. 機能として正しく動くか

**3 を犠牲にしても 1 を守る。**

バックエンドも同様に重い。捕捉されない例外はホスト上の**全フローを止める**。

## 繰り返している失敗

**テスト環境と実環境の乖離。** これが原因の不具合が3回出ている。

- 生成コードを `new Function` で実行するテストは function ノードとグローバルが違う（`require` は存在しない）
- 通信をスタブしたテストは通信層を検証しない（UI テスト103件が全通過する一方、実ブラウザでは動かなかった）
- ログを抑制したテストはログの不具合を見つけられない

**新機能には、実環境で動くことを確かめるテストを必ず1つ書く。**

**テストを追加したら、実装を外して失敗することを確認する。** 通っているだけのテストは何も守っていないことがある。

## よく使うコマンド

```
npm run lint          # js と html の両方
npm test              # 生成器の単体テスト
npm run test:ui       # エディタ UI（jsdom）
npm run test:browser  # 実ブラウザ。BROWSERS=chromium,firefox,webkit
npm run test:plugin:unit  # バックエンドのルート
npm run test:live     # 実 API に対して curl と Node-RED を比較
```

`test:browser` と `test:live` は時間がかかる。`node --test --test-name-pattern="..."` で絞れる。

## 注意

- **`live` は httpbingo のみ**に対して動く。第三者 API を足すと、その停止・レート制限・変動値で CI が赤くなる。
- **リリースは全 CI ジョブの成功が前提**（`release` の `needs`）。赤いままタグを打っても起動しない。
- 生成コードは**プレースホルダを残す**。それを検査せず配備できてしまう点は未解決の弱点。
- 秘密情報を出力に含めない。URL の資格情報は `redact()` を通す。
