# アーキテクチャに関する覚書

この文書は、本プラグインを業務システムで使うかどうかを判断する方と、次にこのコードを保守する方に向けています。**フローエディタのクラッシュ、バックエンドのクラッシュ、ファイルシステムへの破壊的な変更**の3点を中心に、実際にコードを確認して分かったことを書きます。推測は推測と明記します。

---

## 前提：何が最悪の障害か

**このプラグインで最も重大な障害は、生成コードの誤りではなく、フローエディタが動かなくなることです。** エディタが壊れればフローを開発も修正もできません。稼働中のシステムに問題が起きたとき、**直す手段そのものを失います。** 生成コードの誤りは1つのフローに閉じますが、エディタの障害はすべての開発と、あらゆる緊急対応を止めます。

| 障害 | 影響範囲 | 深刻度 |
| --- | --- | --- |
| **エディタが開かない・操作できない** | **すべてのフロー、すべての開発、緊急対応そのもの** | **最悪** |
| **Node-RED プロセスが落ちる** | **そのホスト上の全フローが停止** | **最悪** |
| ファイルシステムの破壊 | 環境全体、復旧に時間を要する | 極大 |
| 生成コードが誤ったリクエストを送る | そのフロー1つ | 高 |
| API Spec タブが出ない | このプラグインの機能のみ | 低 |

最終行が示すとおり、**このプラグインが使えなくなること自体は軽微です。** 標準のインポート機能は残り、フローは手で書けます。だからこそ、機能を守るために上位のリスクを取ることは決して割に合いません。

---

## 1. ブラウザ側：エディタがクラッシュするリスク

### 検証済み：プラグインの故障はエディタに波及しない

エディタは `flowgen.js` を `generator.js` として HTTP 取得します。この取得が失敗したり壊れたスクリプトが返ったりしたときの挙動を実測しました。

200 を返しつつ中身が壊れている4パターン（構文エラー、読み込み時に throw、空ファイル、HTML が返る）を注入した結果：

- エディタは生存し、`RED.actions` / `RED.view` / `RED.nodes` すべて健在
- **ページエラーはゼロ**
- API Spec タブが消えるだけ

`$.getScript` の失敗が Promise チェーンで吸収され、`sync()` が `uninstall()` を呼ぶためです。**この「壊れたら黙って消える」性質が、本プラグインで最も価値のある安全特性です。** `tests/test-browser.js` にこれを守るテストがあります。**壊さないでください。**

### エディタ側コードは意図的に保守的

`flowgen-plugin.html` はエディタのブラウザ内で動きます。ここには**アロー関数が1つもありません。** `var` と `function` だけです。一方、ブラウザに配信される `flowgen.js` にはアロー関数が124箇所あります。

この非対称は妥当です。**エディタ内で動くコードが対応ブラウザで構文エラーを起こすと、スクリプト全体が読み込まれず、そこから先の処理がすべて失われます。** 新しい構文を使うときは対応ブラウザの下限を意識してください。偶然ではなく、維持すべき設計判断です。

### 未検証のリスク

- **巨大な仕様を読み込んだときのブラウザ応答性。** 現状パースはブラウザ側で行われ、`flowgen.js` と js-yaml のブラウザ版をダウンロードして実行します。数千エンドポイントの仕様や数十 MB の YAML を貼り付けたときにタブが固まるかは**未検証**です。エディタが固まれば、それは失われた開発時間そのものです。**パースをバックエンドへ移せば、この risk と配信そのものが同時に消えます。**
- **DOM への割り込み。** プラグインは Node-RED のインポートダイアログ（`#red-ui-clipboard-dialog`）に `dialogopen` フックで割り込み、タブを挿入します。**これは Node-RED が公開している拡張点ではなく、内部 DOM 構造への依存です。** Node-RED がダイアログ構造を変えれば、タブが出ない、あるいは二重に出る可能性があります。エディタ自体は壊れませんが、この依存は認識しておくべきです。ブラウザテストが実際にこの構造を叩いているのが唯一の防御線です。
- **テストが歩く経路しか検証していない。** Chromium・Firefox・WebKit・Edge を網羅しているのは良い状態ですが、未検証の操作手順でエディタが壊れる可能性は残ります。

### エディタ側を変更するときの判断順序

1. **その変更が失敗したとき、エディタは生き残るか。** 生き残らないなら設計をやり直してください。
2. **対応ブラウザすべてで構文エラーにならないか。** 迷ったら `var` と `function` を使ってください。
3. 機能として正しく動くか。

**3 を犠牲にしても 1 を守る価値があります。機能が使えないのは不便ですが、エディタが開かないのは事故です。**

---

## 1-b. インストール・アンインストール・バージョンアップの繰り返し

プラグインはシステムの生涯を通じて、導入され、外され、上げ直されます。**各サイクルが痕跡を残すなら、劣化は繰り返すたびに蓄積します。** タブが二重に出る、古いペインが残る、イベントハンドラが積み上がる――いずれもエディタを少しずつ壊します。

### 検証済み：8サイクル繰り返しても痕跡は残らない

プラグインの有無を切り替えながらインポートダイアログを8回開閉し、毎回 DOM を数えました。

| 状態 | タブリンク | ペイン | ダイアログのタブ総数 | 選択中タブ | 表示中ペイン |
| --- | --- | --- | --- | --- | --- |
| 導入時 | 1 | 1 | 4 | 1 | 1 |
| 除去時 | 0 | 0 | 3 | 1 | 1 |

8回すべてでこの通りであり、**ページエラーはゼロでした。** 重複も残骸も発生しません。

これは `uninstall()` が丁寧に後始末しているためです。挿入した `li` とペインを削除し、`dialogclose.flowgen` ハンドラを外し、OK ボタンに付けた `data-flowgen-bound` 属性を消し、**自分のタブが選択中だった場合は標準のクリップボードタブを選び直します。** 最後の処理がないと、プラグインを外した瞬間に「どのタブも選択されていないダイアログ」が残ります。

`install()` も冒頭で `if (tabs.find('#flowgen-tab-link').length) { return; }` と重複挿入を防いでいます。

この性質は `tests/test-browser.js` のテストで固定しました。**エディタ側のタブ挿入・削除に手を入れるときは、このテストを必ず通してください。**

### 永続状態を持たない

コードを確認した限り、プラグインは **`localStorage`、`sessionStorage`、Node-RED の context、`settings.js`、ユーザディレクトリのいずれにも何も書き込みません。** バックエンドがファイルを書くのは `git clone` 用の一時ディレクトリだけです。

したがって**アンインストール後に残る設定や状態はありません。** バージョンアップ時に古い形式のデータが読まれて壊れる、という類の問題は構造的に起こり得ません。**これは安心して良い点です。**

### バージョン混在について

ブラウザは `flowgen.js` を `generator.js` として取得します。**バージョンアップ直後、ブラウザが古い `generator.js` をキャッシュしたまま新しいバックエンドと話す**組み合わせは理屈の上ではあり得ます。

ただし**これが実害を生むかは検証していません。** 現在のバックエンドの API は単純で（ファイルを取得して返すのみ）、生成処理はすべてブラウザ側で完結するため、深刻な不整合は起きにくい構造に見えます。**確証がないため、リスクとして断定はしません。** 気になる場合は、バージョンアップ後にエディタをリロードしてください。

---

## 2. バックエンド：Node-RED プロセスが落ちるリスク

**ここが最も注意を要する部分です。** プラグインのバックエンドは Node-RED 本体と同じプロセスで動きます。**ここで捕捉されない例外が出れば、Node-RED ごと落ちます。そのホストの全フローが止まります。**

### 入力サイズに上限がない

`/flowgen/collection` は zip アップロードを受け取りますが、**リクエストサイズの上限を設定していません。** 展開後の内容は `Buffer.concat` でメモリに載ります。

- 巨大な zip、あるいは**展開すると膨張する zip（zip bomb）** を投げられると、メモリを食い潰して**プロセスが OOM で落ちます。**
- zip 自体はディスクに展開せずメモリ上で読むため **Zip Slip（`../` によるパス脱出）は成立しません。** これは良い設計です。
- ただし**メモリ上で完結することが、そのまま OOM リスクになっています。**

**対策：** アップロードサイズの上限、展開後の合計サイズの上限、エントリ数の上限を設けるべきです。現状どれもありません。

### `/flowgen/source` は任意の URL を取りに行く

エディタから渡された URL をバックエンドが取得します。認証は `RED.auth.needsPermission('flows.write')` のみです。

- **SSRF。** `http://169.254.169.254/`（クラウドのメタデータ）や社内ネットワークのアドレスを指定でき、**Node-RED が到達できる範囲すべてに、Node-RED の権限でアクセスできます。** ホスト名の検証はありません。
- リダイレクトは5回まで追従します。追従先の再検証はありません。
- タイムアウトは設定されていますが、**レスポンスサイズの上限がありません。** 巨大なファイルを返す URL を指定されるとメモリを消費します。

**対策：** 少なくともレスポンスサイズの上限と、プライベート IP レンジへの接続拒否を検討すべきです。

### git clone

`.git` で終わる URL は `git clone` されます。

- `execFile` を使っており**シェルを経由しないため、コマンドインジェクションは成立しません。** URL は `^https?://\S+$` で検証済みです。これは良い設計です。
- タイムアウト60秒、`--depth 1` を指定しており、失敗時は full clone にフォールバックします。
- **クローンサイズの上限はありません。** 巨大なリポジトリを指定されるとディスクを埋めます。

### 例外処理

ルートハンドラ内の `try` は2箇所のみです。**非同期コールバック内で投げられた例外は Express のエラーハンドラに届かず、`uncaughtException` になります。** Node-RED の既定設定では、これはプロセス終了を意味します。

**このリスクは実際に検証していません。** 落ちる経路が存在するかは、意図的に例外を起こす入力を作って確かめる必要があります。**architecture 上の懸念として挙げておきますが、断定はしません。**

---

## 3. ファイルシステムへの破壊的な変更

### 書き込む場所

コードを読んだ限り、プラグインがファイルを**書き込む**のは1箇所だけです。

```js
const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'flowgen-git-'));
```

`mkdtempSync` は一意なディレクトリを新規作成します。**既存ファイルを上書きする経路はありません。** ユーザのフローファイルにも `settings.js` にも触れません。**これは安心して良い点です。**

### 削除する場所

```js
fs.rm(tmp, { recursive: true, force: true, ... })
fs.rmSync(tmp, { recursive: true, force: true, ... })
```

`recursive: true, force: true` は強力な削除です。**削除対象が `mkdtempSync` の戻り値のみである限り安全です。** 現状そうなっています。

**保守上の注意：** この `tmp` に外部由来の値が混ざるような変更を加えると、**任意のディレクトリを再帰削除できてしまいます。** ここは触らないでください。触る必要が生じたら、削除前に対象がテンポラリ配下であることを検証してください。

### 確認された問題：シンボリックリンクを辿る

`gather()` はクローンしたディレクトリを再帰的に走査し、`.bru` / `.yaml` / `.json` を読みます。**シンボリックリンクの判定がありません。**

実際に検証しました。クローン先に `link.yaml -> /tmp/symtest/outside.yaml` を置いたところ、**リンク先の内容がそのまま読み出されました。**

```
[{ "path": "link.yaml", "text": "secret: value\n" }]
```

つまり、**悪意あるリポジトリが `config.yaml -> /etc/passwd` や `key.json -> ~/.ssh/id_rsa` のようなリンクを含めていれば、その内容がエディタに返ります。** git はシンボリックリンクをそのままクローンします。

これは**読み出しのみで、書き込みや削除は起きません。** ファイルシステムを破壊するわけではありませんが、**情報が漏れる経路**です。

**対策：** `entry.isSymbolicLink()` を除外するか、`fs.realpathSync` で解決した先がクローン先の配下にあることを確認してください。数行で塞げます。

### 一時ディレクトリの後始末

`finally` で削除しているため、正常系・異常系とも回収されます。ただし **`fs.rm` の完了を待たず、失敗しても無視します**（コールバックが空）。削除に失敗した場合、一時ディレクトリが残り続けます。長期稼働で蓄積しうる点は認識しておいてください。

---

## テストの網羅状況

全スイート合わせて約380件のアサーションがあります。

**十分に網羅されている領域**

- 幅広い定義に対する生成コードのテキスト
- `.bru` / YAML / JSON 各形式の Bruno コレクション
- 4つのブラウザエンジンにわたるエディタ操作
- 壊れたプラグイン資産に対するエディタの生存
- 実エディタから実測した幅に基づくグリッド整合

**手薄な領域 — 次の不具合を探すならここ**

1. **エディタの操作経路。** 最優先事項でありながら、テストが歩く経路しか検証していません。**ここを増やすことが最も価値の高いテスト投資です。** 複数フローを開いた状態での操作、大きな仕様を読み込んだ直後の応答性、ダイアログを開閉しながらの連続操作、インポート中のダイアログ強制クローズなどが未検証です。
2. **バックエンドの異常入力。** zip bomb、巨大アップロード、不正な zip、巨大リポジトリ、到達不能な URL、リダイレクトループ。**プロセスが落ちるかどうかを確かめるテストがありません。** これは最悪級の障害に直結する領域なのに、空白です。
3. **実 Node-RED ランタイム上での生成コード実行。** テストの大半は `new Function` で実行しており、**Node-RED の function ノードとはグローバルが異なります。** 実際にこの隙間から `require is not defined` の不具合が漏れました（`new Function` では通り、function ノードでは throw）。
4. **並行性。** すべてのランタイムテストは1つのフローに1メッセージを流すだけです。
5. **生成コードへのインジェクション。** 定義はテンプレートリテラル内に埋め込まれる文字列を制御できます。`quote()` はバッククォートと `${` をエスケープしますが、**構築による安全確保であって、性質としてテストされてはいません。** 信頼できない出所の定義は実行可能な入力です。

---

## バージョン依存性

### Node-RED

- **下限は `>=4.0.3`。** Node.js 26.7.0 の下で 3.0.0〜5.0.4 を二分探索した結果、**4.0.2 以前はすべて動きません。** 原因は `util.log()` で、Node.js 22 で削除された API を Node-RED 3.x と 4.0.2 以前のロガーが呼んでいます。
- **この失敗の現れ方に注意してください。** ログレベルを `fatal` に設定して抑制していると、`util.log` を呼ぶ経路に入らないため**一見正常に起動します。** 当初の調査はこれで「3.0.0 で動作する」と誤判定しました。**壊れるのは、何か問題が起きてログを書こうとした瞬間です。** 問題を調べようとした瞬間に調べる手段を失う、最も質の悪い壊れ方です。
- **`functionExternalModules` が無効の環境**では function ノードの `libs` が使えません。そのため digest の署名ノードは外部モジュールに依存せず MD5 を自前で持っています。無効化された環境でも動作することを実 Node-RED 上で確認済みです。

### Node.js

- **下限は `>=18.5.0`。** 対象とする Node-RED 4.1.13 が `>=18.5` を宣言しており、18.5.0〜26.7.0 の199リリースを二分探索して、下限そのもので動作することを実測しています。
- 判定は「Node-RED が起動する」ではなく、**実ブラウザでエディタを開き、仕様を貼り付けてエンドポイント一覧が描画されるところまで**です。エディタが動かない runtime はサポート対象ではありません。
- Node-RED 4.1.13 は 18.4.0 でも起動しますが、**`npm install` が `EBADENGINE Unsupported engine` を警告します。** 上流が保証しない組み合わせを宣言によって推奨すべきではないため、下限には採用していません。
- **下限は対象 Node-RED のバージョンとセットでしか意味を持ちません。** Node-RED 5.0.4 は `>=22.9` を宣言しており、そちらを対象にすれば下限は 22.9.0 に上がります。
- CI の `node-floor` ジョブが chromium / firefox / webkit の3エンジンで宣言下限を毎回検証します。

---

## 人命に関わる系で使う場合について

その用途で、生成されたフローを**レビューなしに使うことは推奨しません。**

本プラグインの目的は、仕様から**出発点**を作ることです。人が埋めるためのプレースホルダを残し、任意項目をコメントアウトし、ドキュメントの example 値を写します。これらは足場作りの道具としては正しい選択であり、**検査なしに配備されるものとしては誤った性質です。**

特に危険なのは**静かに失敗する**ものです。

- **プレースホルダがそのまま送信されうる。** `{petId}` を残したままデプロイすることを妨げるものはありません。**`DELETE` でパスパラメータが未置換のまま送られるのが最悪のケース**で、形式的には正常なリクエストが誤ったリソースに対して実行されます。
- **任意項目がコメントアウトされたまま残る。** API が実際には必要とする項目が欠けたまま、リクエストは成功します。ステータスコードには現れません。
- **example 値が本番値に見える。** どの値を人間が選び、どの値をドキュメントから写しただけなのかを見分ける手段がありません。

### 導入するなら

- **生成されたフローはデプロイ前に必ずレビューし、実エンドポイントに対して検証すること。** システムを知らない同僚が書いた下書きとして扱ってください。
- **function ノードに `{プレースホルダ}` が残っていたらデプロイを失敗させる検査を入れること。** 現在存在せず、追加は容易です。**ここで取りうる安全策のうち費用対効果が最も高いものです。**
- **バックエンドを外部に晒さないこと。** `/flowgen/source` は Node-RED が到達できる範囲すべてに、Node-RED の権限でアクセスできます。信頼できない利用者がエディタを操作できる環境では、このプラグインを入れるべきではありません。
- **開発環境と本番環境でプラグインの導入を分けることを検討すること。** 本番の Node-RED にこのプラグインが必要な理由は通常ありません。**入れなければ、上に挙げたバックエンドのリスクはすべて消えます。**

最後の項目が、おそらく最も効果的です。**これはコードジェネレータであり、開発時の道具です。** 本番稼働するホストに置く必然性は薄く、置かない判断はいつでも取れます。
