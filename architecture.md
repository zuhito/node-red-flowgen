# アーキテクチャに関する覚書

この文書は、本プラグインを業務システムで使うかどうかを判断する方と、次にこのコードを保守する方に向けて書いています。問題が起きうる箇所については率直に書きました。ここで本当に怖い失敗はクラッシュではなく、**エディタ上は正しく見えるフローが、本番で誤ったリクエストを送ること**だからです。

## このプラグインが実際に行っていること

`flowgen.js` は API 定義（OpenAPI 3、Swagger 2、Bruno コレクション）を読み、**JavaScript 関数のソースコードそのもの**と、それを包む Node-RED フローを出力します。

```
inject → function（msg.method/url/headers/payload を組み立てる） → http request → debug
```

この設計から2つの性質が導かれ、以下のリスクはすべてここに起因します。

**出力はデータ構造ではなくソースコードです。** 誰も型検査をしません。ジェネレータの誤りは、そのまま Node-RED が受け入れて実行してしまう文字列になります。エディタ上はどちらの場合も普通の function ノードに見えます。

**ジェネレータは3つの異なる環境で動きます。** Node（CLI とテスト）、ブラウザ（インポートダイアログが `flowgen.js` を `generator.js` として HTTP 取得する）、そしてその**出力**は Node-RED の function ノード内で動きます。この3つは利用できるグローバルが同じではありません。**これが本リポジトリで最も多くの不具合を生んできた原因です。**

## 最も危険な性質：テスト環境は実環境ではない

ジェネレータのテストのほとんどは、生成コードを次のように実行しています。

```js
function run(code) {
    return new Function('msg', code)({});
}
```

`test.js` だけで `flowgen.generate` を135箇所呼び、このヘルパで実行しています。高速で、多くの誤りを捕まえられます。**しかし Node-RED の function ノードとはグローバルが異なり、ここを通ったコードが実行時に落ちることがあります。**

これは仮定の話ではありません。digest 認証の署名ノードは以前こう生成されていました。

```js
const crypto = require("crypto");
```

Node 上の `new Function` では通ります。しかし Node-RED の function ノードでは即座に `ReferenceError: require is not defined` になり、**署名処理は一度も動きようがありませんでした。** 単体テストは緑のままでした。実際に Node-RED を起動してログを見て初めて発覚しました。

稼働中の Node-RED 4.x で実測した差異です。

| グローバル | Node の `new Function` | Node-RED の function ノード |
| --- | --- | --- |
| `require` | 使える | **未定義** — ノードの `libs` を使う |
| `Buffer` | 使える | 使える |
| `URL` | 使える | 使える |
| `global` / `flow` / `context` | 未定義 | 使える |

**ここから導かれる原則：** 生成コードが素の JavaScript・`Buffer`・`URL` を超えるものを使う場合、**実際の Node-RED ランタイムに読み込ませるテストで裏付けを取ること。** `tests/test-nodered.js` と `tests/test-digest.js` はこれを行っていますが、大半のテストは行っていません。

## 本番で問題になりうる失敗

静かに失敗するものほど危険なので、その順に並べます。

### 1. プレースホルダがそのまま送信されうる

生成コードは `{petId}`、`{token}`、`{api-key}` を意図的に残し、人が埋める前提です。**しかしそのままデプロイすることを妨げるものは何もありません。** 結果として `{token}` という文字列を資格情報として送ったり、URL に `{petId}` を含んだまま送信したりします。

サーバの反応は様々で、拒否するものもあれば、実在の識別子として扱うものもあります。**本リポジトリで最悪のケースは、`DELETE` でパスパラメータが未置換のまま送られることです。** 形式的には正常なリクエストが、誤ったリソースに対して実行されます。

実行時のガードはありません。生成コードは送信前に自らのプレースホルダを検査しません。意図的な簡潔さではありますが、出力を信頼する前に知っておくべき性質です。

### 2. 任意項目はコメントアウトされる

リクエストボディについて、ジェネレータは必須プロパティを有効なコードとして、任意プロパティをコメントアウトして出力します。「任意」かどうかはスキーマ由来ですが、**現実のスキーマはしばしば不正確または不完全です。** API が実際には必要とする項目が、黙ってコメントのまま残ることがあります。

その結果、部分的なボディでリクエストが成功します。**ステータスコードにはこれは現れません。**

### 3. example の値が本物の値になる

定義の `example:` と `default:` はそのままペイロードにリテラルとして書き込まれます。`"model": "gemma3:4b"`、`"status": "available"` といった値は、もっともらしい本番値に見えます。フローをレビューする人が、**どの値を人間が選び、どの値をドキュメントから写しただけなのかを見分ける手段がありません。**

### 4. inject ノードのペイロード

inject ノードは既定で `msg.payload` にタイムスタンプを載せます。ボディを持ちうるメソッドでは、生成コードが消さない限りこのタイムスタンプがリクエストボディになります。現在は該当メソッドに `delete msg.payload;` を出力しています。**`DELETE` も含みます** — Node-RED は DELETE でもボディを送ることをローカルサーバで検証済みです。

これは実際にしばらく存在した不具合で、しかも**テスト側が実行前に inject ノードを書き換えていたために隠れていました。** 検証対象を実行前に「直してしまう」テストは、まさに見つけるべき不具合を隠します。

### 5. Digest 認証は2リクエスト構成

Digest は1回のリクエストでは応答できません。生成されるフローはこうなります。

```
function → http request（senderr: true、401 を受け取る）
         → function（チャレンジに署名し、Cookie を引き継ぐ）
         → http request（認証済みの再送）
```

1つ目の `senderr: true` は必須です。これがないと 401 がエラーとして扱われフローが止まります。再送ではチャレンジの Cookie も引き継ぎます。httpbin 系のサーバは nonce を Cookie に紐付けており、Cookie がないと再送を拒否するためです。

`nc` は `00000001` に固定されています。したがって**リクエストごとに nonce カウントの増加を要求するサーバには対応できません。** 単発の呼び出しなら問題ありませんが、厳格なサーバに対する定期実行フローでは問題になります。

## バージョン依存性

### Node-RED

- **function ノードの `libs`** が外部モジュール取り込みの手段です。1.3 より前の Node-RED にはこのフィールドがありません。
- **下限は Node.js のバージョンに強く依存します。** `package.json` は `>=4.0.3` を宣言しています。Node.js 26.7.0 の下で 3.0.0〜4.1.13 を二分探索した結果、**4.0.2 以前はすべて起動できません**。原因は `util.log()` で、Node.js 22 で削除された API を Node-RED 3.x および 4.0.2 以前のロガーが呼んでいます。境界は 4.0.2（失敗）／4.0.3（成功）です。
- この失敗の現れ方には注意が必要です。**ログレベルを `fatal` に設定して抑制していると、`util.log` を呼ぶ経路に入らないため一見正常に起動します。** 実際、当初の調査はこれで「3.0.0 で動作する」と誤判定しました。**壊れているのは、何か問題が起きてログを書こうとした瞬間です。** `tests/find-node-red-floor.js` は意図的にロガーを叩いて確認します。
- 古い Node.js を使う場合、下限はこれより下がります。**この数字は Node.js のバージョンとセットでしか意味を持ちません。** 対象環境が変わったら `NODE_BIN=... node tests/find-node-red-floor.js` で測り直してください。
- **`functionExternalModules`** はサイトの `settings.js` で無効化されている場合があります。無効だと function ノードの `libs` は `Function node not allowed to load external modules` で失敗します。**このため digest の署名ノードは外部モジュールに依存せず、MD5 を自前で持っています。** 無効化された環境でもフローが 200 に到達することを `tests/test-digest.js` が実 Node-RED 上で確認しています。
- **ノード配置**はエディタがラベル幅を実測することに依存します。この幅は近似式ではなく、稼働中のエディタからブラウザテストで読み取っています。近似はずれてノードがグリッドから外れるためです。エディタのフォントが変わる Node-RED リリースでは実測値も変わります。**それを検知するのがブラウザテストなので、これは残してください。**
- **`http request` の `method: "use"`** は `msg.method` からメソッドを取ります。安定していますが、プラグインが所有しないノードとの暗黙の契約です。

### Node.js

- 生成コードはテンプレートリテラル、アロー関数、`const` などを使います。いずれも長く安定しており、特殊なものはありません。
- Basic 認証で `Buffer.from(...)` を使います。安定しています。
- むしろ**プラグイン自身のコード**の方が影響を受けやすい位置にあります。zip の展開、`git` の起動、ファイルシステムの読み取りを行っており、これらは非推奨化のあった API に触れます。

### ブラウザ

インポートダイアログは現在 `flowgen.js` と js-yaml のブラウザ版をダウンロードし、**パースをクライアント側で行っています。** ここが本プロジェクトで最もブラウザ依存の広い部分であり、最も縮小する価値のある箇所です。

- ブラウザで YAML をパースするということは、対応するすべてのブラウザでパーサ全体を動かすということです。**この処理をバックエンドへ移せば、`generator.js` と `yaml-parser.js` の配信自体が不要になります。**
- エディタ UI は `user-select`、`scrollIntoView({ block: 'nearest' })`、`metaKey`/`ctrlKey` の扱い、jQuery イベントを使っています。ブラウザテストは Chromium・Firefox・WebKit・Edge を網羅しており良好ですが、**テストがたどる経路に限った話です。**

## テストの網羅状況

全スイート合わせて約380件のアサーションがあります。率直な評価です。

**十分に網羅されている領域**

- 幅広い定義に対する生成コードのテキスト（`test.js` だけで135回の生成呼び出し）
- `.bru`・YAML・JSON 各形式の Bruno コレクション
- 4つのブラウザエンジンにわたるエディタ操作
- 実エディタから実測した幅に基づくグリッド整合
- `@usebruno/converters` による相互変換と結果比較

**手薄な領域 — 次の不具合を探すならここ**

1. **実 Node-RED ランタイム上での生成コード実行。** `test-nodered.js` の数エンドポイントと digest フローだけが実際に Node-RED に読み込まれます。残りはすべて `new Function` です。**この隙間が `require` の不具合を生みました。同種の次の不具合も、ここから出ます。**

2. **生成フローのエラー経路。** テストは正常系を検証しています。500、タイムアウト、接続リセット、リダイレクト、非 JSON のボディに対してフローがどう振る舞うかはほぼ未検証です。**本番ではこれらが日常的に起きます。**

3. **巨大な定義・敵対的な定義。** 数百エンドポイントの仕様、深くネストした `$ref`、循環参照する `$ref`、50MB のアップロードを扱うテストがありません。**zip ハンドラもパーサも信頼できない入力を受け取ります。**

4. **並行性。** すべてのランタイムテストは1つのフローに1メッセージを流すだけです。2つのメッセージが同時に届いた場合、`msg` の扱いと、digest では `nc` カウンタが共有されます。未検証です。

5. **生成コードへのインジェクション。** 定義は、テンプレートリテラル内に埋め込まれる文字列を制御できます。`quote()` はバッククォートと `${` をエスケープしますが、これは構築による安全確保であって、性質としてテストされてはいません。**信頼できない出所の定義は実行可能な入力です。** ファジングやプロパティベースのテストはありません。

6. **`live` スイートは到達性を確認するもので、正しさの網羅ではありません。** curl と Node-RED を比較する強力な検査ですが、現在は単一サービス（httpbingo）に対して動いています。安定性のために網羅性を手放しました。CI としては正しい判断でしたが、**変わった実世界の仕様をジェネレータがどう扱うかは、もはやこのスイートからは分かりません。**

7. **未置換のプレースホルダを送らないことを検証するテストはありません。** ジェネレータがそれを行わないためです。失敗パターン1を参照してください。

## 人命に関わる系で使う場合について

その用途で、生成されたフローを**レビューなしに使うことは推奨しません。** 曖昧に濁さず理由を述べます。

本プラグインの目的は、仕様から**出発点**を作ることです。人が埋めるためのプレースホルダを残し、任意項目をコメントアウトし、ドキュメントの example 値を写します。これらは足場作りの道具としては正しい選択であり、**検査なしに配備されるものとしては誤った性質です。** このツールは「この example 値は問題ない」と「この example 値は誤ったレコードを削除する」を区別できません。その区別は仕様に書かれていないからです。

具体的には、以下を求めます。

- **生成されたフローは、デプロイ前に必ずレビューし、実エンドポイントに対して検証すること。** システムを知らない同僚が書いた下書きとして扱ってください。
- **function ノードに `{プレースホルダ}` が残っていたらデプロイを失敗させる検査を入れること。** これは現在存在せず、追加は容易です。**ここで取りうる安全策のうち、費用対効果が最も高いものです。**
- **コメントアウトされたボディ項目を、意図をもって取捨すること。** 生成されたまま放置しないでください。
- **`nc` の扱いを再検討すること。** digest 認証のフローを繰り返し実行する前に必要です。
- **対象の Node-RED で `functionExternalModules` が有効であることを確認すること。** digest を使う場合に必要です。

これらは本プラグインを不適格にするものではありません。**これはコードジェネレータであるという、そのままの事実を意味します。** リスクはジェネレータが間違うことにあるのではなく、**出力が完成品に見えてしまうことにあります。**
