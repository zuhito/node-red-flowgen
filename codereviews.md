# コードレビュー

`flowgen.js`（1592行）、`flowgen-plugin.js`（326行）、`flowgen-plugin.html`（803行）を読んだ結果です。

初めてこのコードに触れる方にも分かるよう、**なぜそれが問題なのか**から書いています。指摘の重さは 🔴 重大 / 🟡 要改善 / 🟢 小さな改善 で示します。

---

## 全体の構造

まず、このプラグインが何をしているかを押さえてください。

```
[ブラウザ]  flowgen-plugin.html   エディタの画面。仕様を貼り付ける UI
     ↓ HTTP
[サーバ]    flowgen-plugin.js     Node-RED と同じプロセスで動く
     ↓ 呼び出し
[共通]      flowgen.js            仕様を読んでコード文字列を作る
     ↓ 出力
[生成物]    function ノードのコード（文字列）
```

**重要な性質が2つあります。**

1. **出力はソースコードそのものです。** データ構造ではないので、型検査も構文検査も効きません。ジェネレータの誤りは「そのまま実行される文字列」になります。
2. **サーバ側は Node-RED 本体と同じプロセスで動きます。** ここで例外が捕まらなければ、**そのホスト上のすべてのフローが止まります。**

以下の指摘は、この2点を基準に重みづけしています。

---

## 🔴 重大：`generateOpenApi3` と `generateSwagger2` の重複

`flowgen.js` で最も長い2つの関数です。

| 関数 | 行数 |
| --- | --- |
| `generateOpenApi3` | 192 |
| `generateSwagger2` | 159 |

このうち **65行が空白や括弧を除いても完全に同一**です。パラメータの組み立て、ヘッダの重複除去、ボディの推定、認証の扱いが、両方に別々に書かれています。

### なぜ問題なのか

**片方だけ直して、もう片方を忘れる**からです。これは仮定の話ではなく、このリポジトリで実際に複数回起きています。

- Bruno 経路で `Content-Type` が欠落 → OpenAPI 経路だけ直っていた
- `.bru` 形式の Basic 認証が `{credentials}` のまま → 他の経路だけ直っていた

どちらもテストが通っている状態で見逃されました。**同じ処理が2箇所にあると、テストも2箇所書かない限り守れません。**

### どう直すか

2つの関数の違いは「**仕様のどこに何が書いてあるか**」だけです。

```
OpenAPI 3: op.requestBody.content['application/json'].schema
Swagger 2: op.parameters.find(p => p.in === 'body').schema
```

この「取り出し方」を関数に分け、取り出したあとの組み立ては共通化できます。

```js
// 方向性のイメージ
function readOperation(doc, method, target) {
    return isSwagger2(doc)
        ? { params: fromSwagger2Params(...), body: fromSwagger2Body(...), auth: ... }
        : { params: fromOpenApi3Params(...), body: fromOpenApi3Body(...), auth: ... };
}
// 以降の組み立ては1本にまとめる
```

**着手する前にテストの安定化を済ませてください。** 大きな構造変更の最中にテストが不安定だと、失敗が退行なのか揺らぎなのか判断できなくなります。

---

## 🔴 重大：生成コードは「未完成のまま動いてしまう」

生成されるコードには、人が埋める前提のプレースホルダが残ります。

```js
msg.method = "DELETE";
// Replace {petId} (integer) in the URL below with a real value.
msg.url = `https://api.example.com/pet/{petId}`;
```

**これを埋めないままデプロイすることを妨げるものが、どこにもありません。**

### なぜ問題なのか

`{petId}` を含む URL に対して `DELETE` が飛びます。サーバによっては 404 を返しますが、**`{petId}` を実在の識別子として扱うサーバもあります。** 形式的には正常なリクエストが、誤ったリソースに対して実行されます。

しかも **ステータスコードには現れません。** 200 が返ってくるだけです。

### どう直すか

生成側で完全に防ぐことはできません（埋めるのは人間の仕事なので）。しかし**デプロイ時に検査する**ことはできます。

```js
// 配備前に function ノードを走査し、{name} が残っていたら止める
if (/\{[A-Za-z_][\w.-]*\}/.test(node.func)) {
    throw new Error(node.name + ' にプレースホルダが残っています');
}
```

数十行で書けて、このリポジトリで取りうる安全策のうち**最も費用対効果が高い**と考えます。

---

## 🟡 要改善：サーバ側の非同期エラー

`flowgen-plugin.js` の `try` は6箇所ありますが、**非同期コールバックの中で投げられた例外はそこに届きません。**

```js
// 危ない形
app.get('/x', (req, res) => {
    try {
        fs.readFile(p, (err, data) => {
            JSON.parse(data);   // ← ここで投げても外の catch には届かない
        });
    } catch (e) { res.status(500).json({}); }
});
```

Express は同期的に投げられた例外しか拾いません。コールバック内の例外は `uncaughtException` になり、**Node-RED の既定設定ではプロセスが終了します。**

### 現状の評価

11通りの異常入力（壊れた JSON、5000段ネスト、循環参照、3000エンドポイント、同時30リクエストなど）で試したところ、**いずれも落ちませんでした。** 現在の `try` の配置は実際にはよく効いています。

ただし**落ちる経路が存在しないことを確認したわけではありません。** 新しいルートを追加するときは、コールバックの中で何が投げうるかを意識してください。

---

## 🟡 要改善：エディタ側のモジュールスコープ変数が多い

`flowgen-plugin.html` の冒頭に11個の状態変数があります。

```js
var loading = null;      var doc = null;
var listed = null;       var selected = null;
var isActive = false;    var anchor = null;
var cursor = null;       var fetching = null;
var collectionDoc = null; var collectionListed = null;
var lastText = null;
```

### なぜ問題なのか

**リセットの漏れがバグになります。** 実際、`doc` と `listed`、`collectionDoc` と `collectionListed` は**必ず対で更新しなければならない**のに、その制約はコードのどこにも書かれていません。片方だけ更新する変更を入れると、一覧と実データが食い違います。

### どう直すか

関連するものをまとめると、リセットが1箇所で済みます。

```js
var state = { doc: null, listed: null, collection: null, collectionListed: null };
function reset() { state = { doc: null, listed: null, collection: null, collectionListed: null }; }
```

**ただし ES5 の範囲で書いてください**（後述）。

---

## 🟡 要改善：ノード幅の推定が実測とずれる

`nodeWidth()` は文字ごとの幅を足してノードの幅を見積もります。

```js
const GLYPH = { ' ': 3, 'a': 8, 'M': 12, ... };
```

エディタで実測したところ、**94文字中93文字は一致**しますが、**ラベル全体で数ピクセルずれます。**

```
文字幅の合計 = 207 / 実測 = 202
```

### なぜ問題なのか

ノードの配置が `x = 左端 + 幅/2` で決まるため、幅が20px違うと**左端が10pxずれてグリッドから外れ**、リンターが警告を出します。実際に `GET /basic-auth/{user}/{passwd}` などで発生しています。

### 原因

**1文字ごとに整数へ丸めているため、誤差が積み上がります。** ブラウザの文字描画は小数精度で、単純な足し算にはなりません。

### どう直すか

文字幅を**小数で**持てば解消します。`'M'×10 + c×10` のような長い文字列から1文字あたりを割り出せば、丸め誤差なく測れます。

なお**実測のテキスト幅を使えば、現在のパディング定数のまま12ラベルすべてが一致する**ことは確認済みです。式は正しく、**テーブルの精度だけが問題**です。

---

## 🟢 良い設計：壊れても黙って消える

エディタは `flowgen.js` を HTTP で取得します。ここが壊れたときの挙動を実測しました。

構文エラー、読み込み時に throw、空ファイル、HTML が返る──**4パターンすべてでエディタは無傷**でした。ページエラーもゼロで、API Spec タブが消えるだけです。

これは `$.getScript` の失敗が Promise チェーンで吸収され、`uninstall()` が呼ばれるためです。

**このリポジトリで最も価値のある安全特性です。** エディタが壊れればフローを直す手段そのものが失われます。テストで守られているので、**この構造を壊さないでください。**

---

## 🟢 良い設計：エディタ側は ES5 で書かれている

`flowgen-plugin.html` の `<script>` には**アロー関数が1つもありません。** `var` と `function` だけです。一方 `flowgen.js` にはアロー関数が124箇所あります。

この非対称は意図的で、正しい判断です。**エディタ内のコードが対応ブラウザで構文エラーになると、スクリプト全体が読み込まれず、そこから先の処理がすべて失われます。**

`npm run lint` が ES5 で検査しているので、アロー関数を1つ書くだけで落ちます。慣習ではなく機械的に守られている点も良いところです。

---

## 🟢 小さな改善：`digestRetryCode` の 77行

digest 認証の署名ノードを生成する関数です。呼び出しは1箇所だけですが、**これは残して正解**です。

展開すると呼び出し側が読めなくなります。「1箇所からしか呼ばれない関数は作らない」という規約はありますが、**規約を守った結果コードが読みにくくなるなら規約のほうが間違っています。**

---

## 🟢 小さな改善：一時ディレクトリの削除失敗が無視される

```js
fs.rm(tmp, { recursive: true, force: true }, () => {});
```

コールバックが空なので、**削除に失敗しても誰も気づきません。** 長期稼働で一時ディレクトリが蓄積します。

致命的ではありませんが、失敗をログに出すか、起動時に古いものを掃除する処理があるとよいでしょう。

---

## 初学者の方へ：このコードから学べること

### 1. テスト環境と実環境は違う

このリポジトリで**3回**同じ形の不具合が出ています。

| 何が起きたか | なぜ見逃したか |
| --- | --- |
| 生成コードが `require("crypto")` を使い、実行時に必ず失敗 | テストは `new Function` で実行しており、`require` が使えた |
| バックエンド移設後、実ブラウザで動かない | UI テストが `$.ajax` をスタブしており、通信層を検証していなかった |
| Node-RED 3.x が Node 22 以降で落ちる | テストがログを抑制しており、`util.log` を呼ぶ経路に入らなかった |

**いずれも単体テストは全通過していました。** 「テストが緑」は「動く」の証明ではありません。

### 2. テストは、外すと落ちることを確認する

git の認証テストが**通っていたのに何も検証していなかった**ことがあります。git は 401 を受けて初めて資格情報を送るため、テストサーバが認証を要求していなければヘッダは届きません。

新しいテストを書いたら、**検証対象の実装を一時的に壊して、そのテストが実際に失敗するか**を確かめてください。

### 3. データが構造として解釈されないようにする

進捗表を `<!-- rows -->` という区切り文字で分割していたところ、**`/rest/v1/neo/browse` の "b<u>rows</u>e" が一致**して表が壊れました。

**データの中身が構造の一部として読まれうる設計は避けてください。**

### 4. 影響範囲で優先順位をつける

| 障害 | 影響範囲 | 深刻度 |
| --- | --- | --- |
| エディタが開かない | すべての開発、緊急対応そのもの | 最悪 |
| Node-RED プロセスが落ちる | そのホストの全フロー | 最悪 |
| 生成コードが誤ったリクエストを送る | そのフロー1つ | 高 |
| このプラグインが使えない | この機能のみ | 低 |

最終行が示すとおり、**このプラグインが使えなくなること自体は軽微です。** 標準のインポート機能は残り、フローは手で書けます。だからこそ、機能を守るために上位のリスクを取ることは割に合いません。

---

## まとめ

着手するなら、この順を勧めます。

1. **プレースホルダ残存の検査**（🔴、小規模）— 安全性への効果が最大
2. **ノード幅テーブルの小数化**（🟡、小規模）— 現に警告が出ている
3. **生成器2つの重複解消**（🔴、大規模）— テストが安定してから
4. エディタ側の状態整理、非同期エラーの見直し（🟡）

3 は差分が大きくなるので、**全エンドポイントの生成結果をハッシュで固定しながら**進めてください。振る舞いが変わっていないことを機械的に確認できます。実際、関数の並べ替えを行った際はこの方法で「48エンドポイントの出力が1バイトも変わっていない」ことを確認できました。
