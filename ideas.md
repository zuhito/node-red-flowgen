# 改善案

半年の作業時間を前提に、効果の大きい順に並べています。各項目は「何が問題か」「なぜそれが問題か」「どう直すか」の順です。

現状（2026-08 時点）:

- `flowgen.js` 1590行 / `flowgen-plugin.js` 296行 / `flowgen-plugin.html` 803行
- テスト約10,000行、行カバレッジ 98.0%、分岐カバレッジ 86.5%
- CI は20ジョブ

---

## 優先度 高

### 1. プレースホルダが残ったままデプロイできる

生成コードは `{petId}`、`{token}` を人が埋める前提で残しますが、**そのまま配備することを妨げるものがありません。** 未置換の `DELETE /pet/{petId}` は、形式的に正常なリクエストが誤ったリソースに対して実行されます。ステータスコードには現れません。

**対策:** デプロイ前に function ノードを走査し、`{name}` が残っていたら失敗させる検査を追加する。Node-RED の `flows` API か、独立した CLI サブコマンドとして提供する。

**規模:** 小（1〜2日）。**このリポジトリで最も費用対効果の高い安全策です。**

### 2. `parseDocument` が604行ある

1つの関数が全ファイルの38%を占めています。YAML/JSON 判定、Bruno の各形式、`$ref` 解決、正規化がすべて入っています。**変更時に影響範囲を読み切れず、分岐カバレッジ86%の未達部分もここに集中しています。**

**対策:** 入力形式ごとに関数を分ける。`parseYaml` / `parseBru` / `parseOpenCollection` / `resolveRefs` など。振る舞いは変えず、全48エンドポイントの生成結果ハッシュで同一性を担保しながら進める。

**規模:** 中（1〜2週間）。テストが厚いので安全に進められます。

### 3. エディタの操作経路のテストが薄い

**エディタが壊れることがこのリポジトリで最悪の事故**ですが、ブラウザテストは決まった経路しか歩いていません。未検証:

- 複数フローを開いた状態での操作
- ダイアログを開閉しながらの連続操作
- インポート中のダイアログ強制クローズ
- 低速ネットワーク下での応答
- 巨大な仕様を読み込んだ直後の操作

**対策:** 上記を1件ずつテストに追加する。特に「処理中に別の操作をする」系は競合を露出させます。

**規模:** 中（2〜3週間）。**投資対効果が最も高いテスト領域です。**

### 4. SSRF とレスポンスサイズ上限

`/flowgen/source` は任意の URL を **Node-RED の権限で**取得します。`169.254.169.254`（クラウドのメタデータ）や社内アドレスに到達でき、レスポンスサイズの上限もありません。

**対策:** プライベート IP レンジへの接続拒否をオプションで提供する。レスポンスサイズに上限を設ける。リダイレクト先も毎回検証する。

**規模:** 小〜中（3〜5日）。**判断が要ります:** 社内 API 定義を読むのが正当な用途なので、既定で塞ぐと機能が壊れます。設定可能にすべきです。

---

## 優先度 中

### 5. `generateOpenApi3` と `generateSwagger2` の重複

192行と159行で、大半が同じ処理です。**片方を直してもう片方を忘れる**事故が実際に起きています（Bruno 経路の Content-Type 欠落、`.bru` の basic 認証）。

**対策:** 差分は「仕様のどこに何が書かれているか」だけなので、そこを抽出する関数を分け、組み立ては共通化する。

**規模:** 中（1週間）。

### 6. テストのタイミング依存

固定待ち（`waitForTimeout`）が原因の不安定さを、ブラウザテストと `find-node-floor.js` の両方で踏みました。**残る固定待ちが同じ問題を再発させます。**

**対策:** `waitForTimeout` の残存箇所を洗い出し、条件待ちに置き換える。CI で同じテストを3回連続実行するジョブを設け、不安定なものを露出させる。

**規模:** 小〜中（3〜5日）。

### 7. 分岐カバレッジ 86.5%

未達の多くはエラー処理経路です。**エラー処理は本番で通る経路**なので、ここが薄いのは実害に直結します。

**対策:** `flowgen-plugin.js` の分岐68.5%を優先する。未到達行を1つずつ潰すのではなく、「この経路はどんな入力で通るか」を考えてテストを書く。

**規模:** 中（1〜2週間）。

### 8. 一時ディレクトリ削除の失敗が無視される

`fs.rm` のコールバックが空で、削除失敗を検知しません。長期稼働で蓄積します。

**対策:** 失敗をログに出す。起動時に古い `flowgen-git-*` を掃除する。

**規模:** 小（1日）。

---

## 優先度 低

### 9. `live` が httpbingo 単一サービスに依存

安定性のために網羅性を手放しました。**変わった実世界の仕様をどう扱うかは、もはや検証されていません。**

**対策:** 実サービスを呼ぶのではなく、**多様な仕様に対する生成結果をスナップショットとして固定する。** 外部依存なしに網羅性を取り戻せます。生成物が変わったときだけ差分が出ます。

**規模:** 中（1週間）。**9 は 3 より優先度が低いですが、外部 API に依存しない点で価値があります。**

### 10. 生成コードへのインジェクションが未検証

仕様は、テンプレートリテラル内に埋め込まれる文字列を制御できます。`quote()` はバッククォートと `${` をエスケープしますが、**構築による安全確保であって、性質としてテストされていません。**

**対策:** プロパティベーステストを入れる。任意の文字列を仕様に埋め込み、生成コードが構文的に妥当で、埋め込んだ文字列が実行されないことを確認する。

**規模:** 中（1週間）。信頼できない出所の定義を読む運用があるなら**優先度は高に上がります。**

### 11. 並行性が未検証

すべてのランタイムテストは1フローに1メッセージです。digest の `nc` カウンタは同時実行で競合しえます。

**対策:** 同一フローに複数メッセージを同時投入するテストを追加する。

**規模:** 小（2〜3日）。

### 12. ドキュメントがない

`README.md` のみで、アーキテクチャ、既知の制約、運用上の注意が残っていません（作成した文書は削除されました）。

**対策:** 必要と判断されるなら再作成する。**不要という判断であればこの項目は無視してください。**

---

## 着手順の提案

半年あるなら、この順を勧めます。

1. **1**（プレースホルダ検査）— 小さく、安全性への効果が最大
2. **6**（タイミング依存）— 以降の作業でテストが信用できる状態にする
3. **3**（エディタ経路）— 最悪の事故を防ぐ領域
4. **2**（`parseDocument` 分割）— テストが厚いうちに構造を直す
5. **5**（生成器の重複解消）— 4 の後なら安全
6. **4**（SSRF）、**7**（分岐カバレッジ）、残り

**2 と 5 を先にやらないでください。** テストが不安定なまま大きな構造変更をすると、失敗が退行なのか不安定さなのか切り分けられなくなります。実際にこのリポジトリで何度か起きています。
