# このリポジトリのコーディング規約

このファイルは、Claude をはじめとする AI アシスタントがこのリポジトリのコードを書くときに従う規約です。人間の開発者にも同じことが当てはまります。

**作業を始める前にこのファイルを読んでください。** 既存コードのスタイルに合わせるだけでは、ここに書かれた意図までは伝わりません。

---

## 1. 数行だけの関数を作らない

数行しかない関数は、名前を付ける価値より、読む人が定義を探しに行く手間のほうが大きくなります。**呼び出し箇所に直接書いてください。**

```js
// してはいけない
function raw(expression) {
    return { __raw: String(expression) };
}
function isRaw(value) {
    return !!value && typeof value === 'object' && typeof value.__raw === 'string';
}

// こう書く
headers.push(['authorization', { __raw: '`Basic ${credentials}`' }]);
// ...
if (value && typeof value === 'object' && typeof value.__raw === 'string') { ... }
```

**目安は本体3行以下**です。これより短いものに名前を付けているなら、それは関数ではなく式です。

## 2. 1箇所からしか呼ばれない関数を作らない

呼び出しが1つなら、その関数は「切り出された」のではなく「移動させられた」だけです。読む人は定義まで飛び、戻ってこなければなりません。**呼び出し箇所に展開してください。**

### ただし例外があります

以下に当てはまるなら、呼び出しが1回でも関数のままにしてください。**規約を機械的に適用してコードを悪くしないでください。**

- **本体が長い**（目安50行以上）。展開すると呼び出し側が読めなくなります。`generateOpenApi3` や `generateSwagger2` がこれにあたります。
- **公開 API である**。`module.exports` から出ているものは、外から呼ばれるので呼び出し数を数える意味がありません。
- **再帰している**。名前がないと書けません。
- **テストが直接呼んでいる**。テストからの呼び出しも呼び出しです。

判断に迷ったら、**展開したあとの呼び出し側が読みやすくなるか**で決めてください。読みにくくなるなら、その関数は残すべきです。

## 3. 規約より、コードが良くなることを優先する

この規約は、意味のない細切れを防ぐためのものです。**規約を守った結果コードが読みにくくなるなら、規約のほうが間違っています。**

そういう箇所を見つけたら、従うのではなく報告してください。

---

## 4. エディタで動くコードは保守的に書く

`flowgen-plugin.html` の `<script>` は Node-RED エディタのブラウザ内で動きます。ここは**特別扱いします**。

- **アロー関数、`const`、`let`、テンプレートリテラルを使わない。** `var` と `function` で書いてください。
- 理由は、**対応する最古のブラウザで構文エラーになれば、スクリプト全体が読み込まれず、プラグインのタブごと失われる**ためです。
- これは慣習ではなく機械的に強制されています。`npm run lint` が ES5 で検査し、アロー関数を1つ書くだけで落ちます。

**エディタが壊れることは、このリポジトリで最悪の事故です。** フローを開発する手段そのものが失われ、稼働中のシステムに問題が起きても直せなくなります。生成コードの誤りは1つのフローに閉じますが、エディタの障害はすべての開発を止めます。

エディタ側を変更するときは、この順で考えてください。

1. その変更が失敗したとき、エディタは生き残るか
2. 対応ブラウザすべてで構文エラーにならないか
3. 機能として正しく動くか

**3 を犠牲にしても 1 を守ってください。**

---

## 5. テスト環境と実環境の違いに注意する

このリポジトリで繰り返し起きている失敗の形です。**同じ間違いを繰り返さないでください。**

- **生成コードを `new Function` で実行するテストは、Node-RED の function ノードとグローバルが違います。** `require` は function ノードに存在しません。過去に `require("crypto")` を生成して、単体テストは全通過のまま実行時に必ず失敗するコードを出したことがあります。
- **通信をスタブしたテストは、通信層を検証しません。** バックエンドへ処理を移した際、`$.ajax` をスタブした UI テスト103件が全通過する一方、実ブラウザでは動きませんでした（Node-RED の admin API が既に body-parser を通しており、`req.body` を使う必要がありました）。
- **ログを抑制したテストは、ログの不具合を見つけられません。** `level: 'fatal'` で検査していたため、Node-RED 3.x が Node 22 以降で `util.log is not a function` で落ちることを見逃しました。

**新しい機能が「実際の環境で動く」ことを確かめるテストを、必ず1つは書いてください。**

---

## 6. テストは、外すと落ちることを確認する

テストを追加したら、**検証対象の実装を一時的に戻して、そのテストが実際に失敗することを確認してください。** 通っているだけのテストは、何も守っていない可能性があります。

過去に、git の認証テストが通っていたにもかかわらず何も検証していなかったことがあります。git は 401 を受けて初めて資格情報を送るため、テストサーバが認証を要求していなければヘッダは届きません。サーバに 401 を返させて初めて、テストが意味を持ちました。

---

## 7. 秘密情報を出力に含めない

エラーメッセージはエディタにそのまま表示され、スクリーンショットやバグ報告に載ります。

- URL に含まれる資格情報（`https://user:token@host/...`）は、**外に出る前に伏せてください**。`flowgen-plugin.js` の `redact()` を使ってください。
- 外部コマンドの `stderr` をそのまま返さないでください。
